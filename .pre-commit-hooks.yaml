- id: sevlint
  name: sevlint (Odoo server action code)
  description: Lint Odoo server action / cron code in module XML and '# sevlint:' .py files
  entry: sevlint check
  language: python
  files: \.(py|xml)$
